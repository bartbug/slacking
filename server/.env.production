# Server Configuration
PORT=4000
NODE_ENV=production

# Database Configuration
DATABASE_URL="postgresql://postgres:${DB_PASSWORD}@${DB_HOST}:5432/slack_clone?schema=public"

# JWT Configuration
JWT_SECRET="${JWT_SECRET}"

# CORS Configuration
CORS_ORIGIN="http://${EC2_PUBLIC_IP}:3000" 